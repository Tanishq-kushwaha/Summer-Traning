/*
 * Program: 2D Matrix Transpose
 * Objective: Read a 2x2 matrix from the user and compute its transpose.
 * Logic: Swap the row and column indices. The element at original[i][j] 
 * becomes the element at transpose[j][i].
 */